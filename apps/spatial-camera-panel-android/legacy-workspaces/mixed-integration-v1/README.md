# Legacy Spatial Camera Panel integration workspace

This workspace preserves public adapter integration history, including the
accepted camera work and the imported Spatial VR Strobe MOD-010 through MOD-012
history. It is not the current project workspace for either consumer.

- Private effect authority: the private-project workspace routed by the repository `AGENTS.md`
- Spatial VR Strobe authority: `apps/spatial-vr-strobe-android/morphospace/`

MOD-013 closes the mixed current-unit state and preserves all earlier source,
receipts, and events. New implementation units must be created in the matching
project workspace.

This v1 ledger predates the current portable validator's closed path,
instruction-surface, and change-category vocabularies. Do not rewrite its
accepted units to satisfy newer schemas. Validate its additive history and
supersession semantics with
`tools/checks/Test-SpatialCameraPanelWorkflowStatic.ps1`; validate current work
with the two protocol-v2 project workspaces above.
